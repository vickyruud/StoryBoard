<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootswatch@4.5.2/dist/sketchy/bootstrap.min.css" crossorigin="anonymous">
   <!-- App CSS -->
   <link rel="stylesheet" href="./styles/layout.css" />
   <link rel="stylesheet" href="./styles/main.css"/> <!--type="text/css" -->

  <title>StoryBoard</title>
</head>
<body>
  <%- include('partials/_header') %>
  <main>
    <section class="story-box">
      <% for (let i = stories.length - 1; i >= 0; i--) { %>
        <article class="story-listing">
          <section class="story-listing__preview">
            <div class = "story-listing__previews__header">
              <h4 class="story-listing__title"> <%= stories[i].title %> </h4>
              <h6 class="story-listing__author"> By: <%= stories[i].author %> </h6>
              <h6 class="story-listing__status"> Status: <%= stories[i].status %></h6>
              <button type = "submit", class = "btn btn-primary", action = "api/storyDetails", method = "POST"> View Story ... </button>
            </div>
            <textarea readonly class = "story-listing__previews__details"> <%= stories[i].contents %>... </textarea>
            <p hidden class="story-listing__id"> <!--<%= stories[i].id %> --></p>
          </section>
        </article>
      <% } %>
    </section>
  </main>
</body>
</html>
